const BACKEND_URL =
  window.location.hostname === "localhost"
    ? "http://localhost:8080"
    : "https://backend-hc.up.railway.app";

const APP_URL =
  window.location.hostname === "localhost"
    ? "http://localhost:3001"
    : "https://app-hc.up.railway.app";